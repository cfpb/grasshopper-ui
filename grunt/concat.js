module.exports = {
    options: {
        separator: ';'
    },
    dist: {
        src: ['js/vendor/*.js', 'js/*.js'],
    // the location of the resulting JS file
        dest: 'dist/js/grasshopper.js'
    }
}